<header class="bg-white shadow">
  <div class="mx-auto max-w-7xl px-2 sm:px-4 lg:divide-y lg:divide-gray-200 lg:px-8">
    <div class="relative flex h-16 justify-between">
      <div class="relative z-10 flex px-2 lg:px-0">
        <div class="flex flex-shrink-0 items-center">
          <%= link_to "Chat App", root_path, class: "btn btn-ghost text-2xl font-bold" %>
        </div>
      </div>
      <div data-controller="dropdown" class="hidden lg:relative lg:z-10 lg:ml-4 lg:flex lg:items-center">
        <!-- Profile dropdown -->
        <div class="relative ml-4 flex-shrink-0">
          <button data-action="dropdown#toggle click@window->dropdown#hide" type="button" class="relative flex rounded-full bg-white focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
            <%= image_tag (current_user.avatar.attached? ? current_user.avatar : "shiba.png"), class: "h-10 w-10 rounded-full object-cover object-center ring ring-white" %>
          </button>

          <div data-dropdown-target="menu" class="hidden transition transform absolute left-0 z-10 mt-2 w-60 divide-y divide-gray-100 rounded-lg border border-gray-100 bg-white text-left text-sm shadow-lg"
               data-transition-enter-from="opacity-0 scale-95"
               data-transition-enter-to="opacity-100 scale-100"
               data-transition-leave-from="opacity-100 scale-100"
               data-transition-leave-to="opacity-0 scale-95">
            <div class="py-3 px-4">
              <div class="flex items-center gap-3">
                <div class="relative h-10 w-10">
                  <%= image_tag (current_user.avatar.attached? ? current_user.avatar : "shiba.png"), class: "h-10 w-10 rounded-full" %>
                </div>
                <div class="text-sm">
                  <div class="text-gray-400"><%= current_user.email %></div>
                </div>
              </div>
            </div>
            <div class="p-1">
              <%= link_to edit_user_path(current_user), class: "flex w-full items-center gap-2 rounded-md px-3 py-2 text-gray-700 hover:bg-gray-100" do %>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-4 w-4">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0zM4.501 20.118a7.5 7.5 0 0114.998 0A17.933 17.933 0 0112 21.75c-2.676 0-5.216-.584-7.499-1.632z"/>
                </svg>
                <%= t(:label_settings) %>
              <% end %>
            </div>
            <div class="p-1">
              <%= button_to destroy_user_session_path, method: :delete, class: "flex w-full items-center gap-2 rounded-md px-3 py-2 text-gray-700 hover:bg-gray-100" do %>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-4 w-4">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0013.5 3h-6a2.25 2.25 0 00-2.25 2.25v13.5A2.25 2.25 0 007.5 21h6a2.25 2.25 0 002.25-2.25V15m3 0l3-3m0 0l-3-3m3 3H9"/>
                </svg>
                <%= t(:label_log_out) %>
                <span class="inline-flex flex-1 justify-end gap-1 text-xs capitalize text-gray-400">
                  <kbd class="min-w-[1em] font-sans">⌥</kbd>
                  <kbd class="min-w-[1em] font-sans">⇧</kbd>
                  <kbd class="min-w-[1em] font-sans">Q</kbd>
                </span>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>
</header>
